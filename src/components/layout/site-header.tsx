// Placeholder for Task 10, which builds the breadcrumb, theme toggle and
// header action button. Kept minimal here so `Shell` compiles.
export function SiteHeader() {
  return <header className="flex h-14 shrink-0 items-center border-b" />;
}
